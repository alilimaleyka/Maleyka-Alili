public interface Loglana {

    //interface · implements
    //general
    //Loglana adlı interface yarat — içində yalnız log() metodunun imzası olsun (gövdəsiz).
    // Servis class-ı bu interface-i
    // implements etsin və log() metodunu tam yazsın. Servis obyekti yaradıб metodu çağır.

    void log ();
}
